package com.Fanuel.divo.ui.theme

import androidx.compose.ui.graphics.Color

// DIVO Brand Colors - Enhanced with gradients and variations
val DivoGreen = Color(0xFF00E676) // Bright green for primary actions
val DivoGreenVariant = Color(0xFF00C853) // Darker green for pressed states
val DivoNeonGreen = Color(0xFF00FF41) // Neon green for logo
val DivoBlue = Color(0xFF2196F3) // Blue for secondary actions
val DivoBlueVariant = Color(0xFF1976D2) // Darker blue for pressed states

// Dark Theme Colors - More sophisticated palette
val DarkBackground = Color(0xFF0A0A0A) // Deeper, richer dark background
val DarkSurface = Color(0xFF1A1A1A) // Slightly lighter surface
val DarkSurfaceVariant = Color(0xFF2A2A2A) // Input fields and cards
val DarkSurfaceVariant2 = Color(0xFF3A3A3A) // Elevated surfaces

// Text Colors - Better contrast and hierarchy
val WhiteText = Color(0xFFFFFFFF) // Primary text color
val WhiteTextSecondary = Color(0xB3FFFFFF) // Secondary text (70% opacity)
val WhiteTextTertiary = Color(0x80FFFFFF) // Tertiary text (50% opacity)
val WhiteTextDisabled = Color(0x61FFFFFF) // Disabled text (38% opacity)

// Status Colors - More vibrant and accessible
val ErrorRed = Color(0xFFFF5252) // Brighter red for errors
val SuccessGreen = Color(0xFF69F0AE) // Brighter green for success
val WarningOrange = Color(0xFFFFB74D) // Orange for warnings

// Gradient Colors for enhanced visual appeal
val GradientStart = Color(0xFF00E676)
val GradientEnd = Color(0xFF00C853)
val GradientBlueStart = Color(0xFF2196F3)
val GradientBlueEnd = Color(0xFF1976D2)

// Accent Colors for special elements
val AccentPurple = Color(0xFF9C27B0)
val AccentPink = Color(0xFFE91E63)
val AccentOrange = Color(0xFFFF9800)
